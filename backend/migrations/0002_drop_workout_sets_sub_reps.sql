-- =============================================================================
-- workout_sets.sub_reps（追加レップ）を廃止
-- =============================================================================
-- 「追加レップ」は不要との判断により列ごと削除する。
-- volume はアプリ側で reps * weight として算出する（sub_reps は加味しない）。
-- 実デプロイは migrate.sh の drizzle-kit push が schema.ts との差分で適用するが、
-- 参照用に同等の DDL を残す。

ALTER TABLE workout_sets DROP COLUMN sub_reps;
