-- =============================================================================
-- workout_sets.position（実績内の表示順）を追加
-- =============================================================================
-- セットの並び順をドラッグ＆ドロップで入れ替え、その順序を永続化するための列。
-- 既存行は DEFAULT 0 で取り込み、同値は created_at で安定ソートする
-- （= 既存データは従来どおり作成順で並ぶ）。新規挿入時はアプリ側が
-- 末尾（その実績内の max(position) + 1）を明示セットする。
-- 実デプロイは migrate.sh の drizzle-kit push が schema.ts との差分で適用するが、
-- 参照用に同等の DDL を残す。

ALTER TABLE workout_sets ADD COLUMN position integer NOT NULL DEFAULT 0;
